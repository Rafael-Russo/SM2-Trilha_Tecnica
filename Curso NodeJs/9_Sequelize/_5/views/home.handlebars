<h1>Usuários cadastrados:</h1>
<div class="container">
  {{#each users}}
    <div class="item">
      <p>{{this.name}}</p>
      <div class="actions">
        <a href="/users/{{this.id}}">Ver detalhes</a>
      </div>
    </div>
  {{/each}}
</div>